var express = require("express");
var app = express();
// //    * `burger.js`
// 4. Create the `router` for the app, and export the `router` at the end of your file.
app.get("/", (req,res)=>{


})

app.post("/burger/add", (req,res)=>{
    

})

app.put("/burger/update", (req,res)=>{
    

})

// module.exports = router